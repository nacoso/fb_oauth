<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name %>
  </div>

  <div class="field">
    <%= form.label :fb_id %>
    <%= form.text_field :fb_id, id: :user_fb_id %>
  </div>

  <div class="field">
    <%= form.label :fb_token %>
    <%= form.text_field :fb_token, id: :user_fb_token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
